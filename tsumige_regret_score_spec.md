# GuiltyStack（積罪）仕様書

> 積んでいるコンテンツの「後悔指数」を自動計算し、毒舌で断罪するWebアプリ

| 項目 | 内容 |
|---|---|
| アプリ名（日本語） | 積罪（つみざい） |
| アプリ名（英語） | GuiltyStack |
| リポジトリ名 | guiltystack |
| 想定URL | guiltystack.vercel.app |
| GitHubリポジトリ | https://github.com/aayofujiwara-tech/GuiltyStack.git |

---

## 1. 概要

### コンセプト
未消化のゲーム・本・マンガ・映画・アニメを登録し、購入からの経過日数・同ジャンル消化率・金額・話題の鮮度から「後悔スコア」を自動計算。スコアに応じた毒舌一言でユーザーを断罪する。

### 対象コンテンツ
- ゲーム
- 本・マンガ
- 映画・アニメ

### プラットフォーム
- WebアプリPWA対応（PC・スマホ両対応）

### 認証・データ
- Supabaseによるアカウント認証 + クラウド同期

---

## 2. 技術スタック

| 層 | 技術 |
|---|---|
| フロントエンド | Next.js（App Router） |
| DB / Auth | Supabase |
| ホスティング | Vercel |
| コンテンツAPI（将来） | RAWG（ゲーム） / Open Library（本） / TMDB（映画・アニメ） |

---

## 3. 後悔スコア設計

### 計算式

```
後悔スコア（100点満点）=
  経過日数スコア  (30点)
+ 消化率スコア   (25点)
+ 金額スコア    (20点)
+ 話題鮮度スコア (25点)
```

---

### ① 経過日数スコア（30点満点）

購入日からの経過日数で加点。**6ヶ月で満点**。

| 経過期間 | 点数 |
|---|---|
| 0〜30日 | 0点（猶予期間） |
| 1〜2ヶ月 | 8点 |
| 2〜3ヶ月 | 16点 |
| 3〜4ヶ月 | 22点 |
| 4〜5ヶ月 | 26点 |
| 5〜6ヶ月 | 29点 |
| 6ヶ月超 | 30点（満点・終了宣告） |

---

### ② 消化率スコア（25点満点）

同ジャンルの「完了済み / 全登録数」の逆数で加点。  
ゲームを10本積んで1本もクリアしていなければ満点に近い。

```
消化率 = completed件数 / 全件数
スコア = (1 - 消化率) × 25
```

---

### ③ 金額スコア（20点満点）

| 購入金額 | 点数 |
|---|---|
| 〜500円 | 2点 |
| 500〜2,000円 | 7点 |
| 2,000〜5,000円 | 13点 |
| 5,000円〜 | 20点 |

---

### ④ 話題鮮度スコア（25点満点）

発売日からの経過月数で加点。「今が旬なのに積んでいる」ほど高スコア。

| 発売からの経過 | 点数 |
|---|---|
| 〜1ヶ月 | 25点（最悪期） |
| 1〜3ヶ月 | 20点 |
| 3〜6ヶ月 | 13点 |
| 6〜12ヶ月 | 6点 |
| 1年超 | 0点 |

**実装フェーズ**

- **v1（手動）**：登録時に発売日を入力
- **v2（自動）**：RAWG / TMDB / Open Library APIで取得  
  → DBに `freshness_source: "manual" | "api"` を持たせ、差し替えのみで対応できる設計にする

---

## 4. スコア帯・レベル定義

| スコア | レベル | アイコン |
|---|---|---|
| 0〜29 | 平和 | 🟢 |
| 30〜59 | 要注意 | 🟡 |
| 60〜79 | 危険 | 🟠 |
| 80〜99 | 末期 | 🔴 |
| 100 | 死亡 | 💀 |

---

## 5. 毒舌テキスト設計

### タグ仕様

各セリフに `tags` を持たせ、コンテンツタイプに合うものだけ抽選する。

```typescript
type RoastLine = {
  text: string
  tags: ("game" | "book" | "manga" | "movie" | "anime" | "all")[]
}
```

`"all"` タグは全ジャンル共通で出る。

### 変数

| 変数 | 内容 |
|---|---|
| `${title}` | タイトル |
| `${price}` | 購入金額 |
| `${days}` | 経過日数 |
| `${months}` | 経過月数 |
| `${per_day}` | 1日あたりの後悔金額（price / days） |
| `${fresh_months}` | 発売からの経過月数 |
| `${undone}` | 同ジャンルの未消化件数 |

---

### 🟢 0〜29点「平和」

| テキスト | タグ |
|---|---|
| 「まだ猶予はある。でも、買ったこと覚えてる？」 | all |
| 「${days}日経過。まだ取り返せる。たぶん。」 | all |
| 「${title}がじっとこっちを見てる。」 | all |
| 「積んだばかりの新鮮な後悔。大事に育てて。」 | all |
| 「セーブデータ、まだ存在すらしてない。」 | game |
| 「パッケージ開封してないやつ、正直に言って。」 | game |
| 「1ページも読んでないのに本棚に並べた。インテリア。」 | book, manga |
| 「まだ1話も見てない。でも気持ちはわかる。」 | movie, anime |

---

### 🟡 30〜59点「要注意」

| テキスト | タグ |
|---|---|
| 「${price}円が${days}日間、完全に眠ってる。」 | all |
| 「同ジャンル、あと${undone}本溜まってるよ。積みのプロかよ。」 | all |
| 「"いつかやる"って言い続けて${months}ヶ月。いつ？」 | all |
| 「${title}の話、もうSNSで誰もしてないけどな。」 | all |
| 「チュートリアルで止まってるやつ、正直に言って。」 | game |
| 「攻略サイトだけ読んでゲームした気になってない？」 | game |
| 「本屋で買った瞬間が一番テンション高かったやつ。」 | book, manga |
| 「積ん読って言葉、自分のためにあると思ってるでしょ。」 | book, manga |
| 「"見るリスト"に入れたまま${months}ヶ月。リストの意味は？」 | movie, anime |
| 「予告編だけ5回見たやつ、本編見てないよね。」 | movie, anime |

---

### 🟠 60〜79点「危険」

| テキスト | タグ |
|---|---|
| 「発売から${fresh_months}ヶ月。ネタバレ踏んでない自信ある？」 | all |
| 「1日${per_day}円ずつ後悔してる計算になる。」 | all |
| 「このまま積み続けると"レトロ作品"扱いになるよ。」 | all |
| 「やらない理由を探すエネルギーで消化できてたのでは。」 | all |
| 「セーブデータの最終更新日、直視できる？」 | game |
| 「そのゲーム、もうオンラインサーバー落ちてない？」 | game |
| 「${title}の続刊、もう完結してるよ。追いつける気する？」 | book, manga |
| 「栞が1巻の序章に刺さったまま${months}ヶ月。」 | book |
| 「シーズン3まで出てるのに1話も見てないの、才能だよ。」 | anime |
| 「劇場版が出たって知ってる？本編まだだけど。」 | anime, movie |

---

### 🔴 80〜99点「末期」

| テキスト | タグ |
|---|---|
| 「積みの域を超えた。これはコレクションと呼ぶべき。」 | all |
| 「中古相場、買値より下がってるかもよ。」 | all |
| 「${title}を知ってる人間が周りから絶滅しつつある。」 | all |
| 「もう"積み"じゃない。"負債"だよこれ。」 | all |
| 「DLC全部出てトロコンしてる人間が地球にいる。あなたは0時間。」 | game |
| 「もはや次世代機で動くか確認が必要な時代になった。」 | game |
| 「その本、図書館で借りれば無料だったやつでは？」 | book, manga |
| 「読んでないのに「面白いよ」って人に薦めたことある？」 | book, manga |
| 「続編映画が公開されて、元の作品が"前日譚"になってる。」 | movie |
| 「声優が変わってるアニメ、気づいてすら居なかったでしょ。」 | anime |

---

### 💀 100点「死亡」

| テキスト | タグ |
|---|---|
| 「おめでとう。${title}は永眠しました。享年${days}日。」 | all |
| 「殿堂入りおめでとう。二度と起動しないと思う。」 | all |
| 「${price}円でお焚き上げしました。合掌。」 | all |
| 「もはや考古学の領域。研究者に寄贈を検討して。」 | all |
| 「そのゲーム、もうオンラインサービス終了してない？確認して。」 | game |
| 「パッケージが日焼けし始めたら本物の積みゲーマー。」 | game |
| 「本の紙、黄ばんでない？匂い嗅いでみて。」 | book |
| 「その巻だけ背表紙の色が新品のまま。一目でわかる。」 | manga |
| 「続編3作公開されて、元作品がクラシック扱いになってる。」 | movie |
| 「そのアニメの監督、次回作もう完成してるよ。」 | anime |

---

## 6. 画面設計

### 画面一覧

| 画面 | 概要 |
|---|---|
| ホーム | 今日の断罪＋全件リスト（同一スクロール） |
| 登録画面 | コンテンツ新規登録 |
| 詳細画面 | スコア内訳・ステータス変更 |
| 統計画面 | 総積み金額・ジャンル別消化率 |

---

### ホーム画面

#### 断罪件数

| デバイス | 断罪件数 | 理由 |
|---|---|---|
| モバイル | 3件（1列） | スクロール前提、縦に並べても自然 |
| PC | 2件（2列グリッド） | 2列で綺麗に収まる、3件目の孤立を避ける |

#### 断罪抽選ロジック（共通・重み付き）

```
抽選重み = 後悔スコア × 1.5
         + 経過日数ボーナス（180日超で +20）
         + 金額ボーナス（5,000円超で +10）
```

スコアが高いほど断罪されやすい。低スコアも稀に選ばれる設計。

#### 統計3項目（共通）

| 項目 | 内容 |
|---|---|
| 総積み額 | 未消化コンテンツの合計購入金額 |
| 平均後悔スコア | 全件の後悔スコア平均 |
| 最古の積み | 最長放置コンテンツのタイトルと経過日数 |

---

#### 📱 モバイルレイアウト

```
┌─────────────────────────────┐
│  💀 積みゲー後悔スコア        │
│  ¥142,800 | 67点 | 最古487日 │  ← タップで統計画面へ
├─────────────────────────────┤
│  🎲 今日の断罪（3件）        │  ← 重み付き抽選・毎日更新
│                             │
│ ┌─────────────────────────┐ │
│ │🎮 Elden Ring   92点 💀   │ │
│ │487日 ¥8,800              │ │
│ │「負債だよこれ。」          │ │
│ │ [✓ 消化した] [→ 詳細]    │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │📚 三体         81点 🔴   │ │
│ │312日 ¥2,200              │ │
│ │「絶滅しつつある。」        │ │
│ │ [✓ 消化した] [→ 詳細]    │ │
│ └─────────────────────────┘ │
│   ・・・（3件目まで）        │
│                             │
│  ── 全件リスト ──           │  ← セクション区切り
│  [後悔順▼] [日付] [金額]    │  ← タブで並び替え
│                             │
│ ┌─────────────────────────┐ │
│ │🎮 FF7R      71点 🔴      │ │
│ │203日 ¥9,000              │ │
│ │「いつ？」                 │ │
│ │ [✓ 消化した] [→ 詳細]    │ │
│ └─────────────────────────┘ │
│   ・・・（スクロール継続）   │
│                             │
└─────────────────────────────┘
  [＋ 追加]                    ← FAB固定（常時表示）
```

**モバイルUXポイント**

- **FAB（＋追加）はフッター固定** — スクロール位置に関わらず1タップで登録
- **「消化した」ボタンをカードに直置き** — 詳細画面不要でステータス変更可能
- **並び替えはタブ切り替え** — ドロップダウンなし、1タップで切替
- **断罪3件 + 全件リストを同一スクロール** — 画面遷移ゼロ
- **統計はヘッダーをタップ** — 統計画面へ遷移

---

#### 🖥️ PCレイアウト

```
┌────────────────────────────────────────────────────────────────┐
│ 💀 積みゲー後悔スコア                            [アカウント]   │ ← 全幅ヘッダー固定
├──────────────┬─────────────────────────────────────────────────┤
│              │                                                 │
│  💀 ホーム   │  🎲 今日の断罪（2件）                           │
│  📋 一覧     │                                                 │
│  ➕ 追加     │  ┌──────────────────────┐ ┌──────────────────────┐│
│  📊 統計     │  │ 🎮 Elden Ring   💀92 │ │ 📚 三体        🔴81 ││
│              │  │ 487日 · ¥8,800       │ │ 312日 · ¥2,200      ││
│  ──────────  │  │                      │ │                     ││
│              │  │「もう"積み"じゃない。 │ │「知ってる人間が      ││
│  総積み額    │  │  "負債"だよこれ。」   │ │  絶滅しつつある。」  ││
│  ¥142,800    │  │                      │ │                     ││
│              │  │ 日数 ████░░  26      │ │ 日数 ██████  30     ││
│  平均スコア  │  │ 消化 ███░░░  18      │ │ 消化 ████░░  20     ││
│  67点 🟠     │  │ 金額 ██████  20      │ │ 金額 ██░░░░   7     ││
│              │  │ 鮮度 ███░░░  18      │ │ 鮮度 ██████  24     ││
│  最古の積み  │  │                      │ │                     ││
│  487日       │  │ [✓ 消化した][詳細 →] │ │ [✓ 消化した][詳細→] ││
│  Elden Ring  │  └──────────────────────┘ └──────────────────────┘│
│              │                                                 │
│              │  ── 全件リスト ─────────────────────────────── │
│              │  [後悔順▼] [日付] [金額]  🔍検索  [ジャンル▼]  │
│              │                                                 │
│              │  ┌──────────────────────┐ ┌──────────────────────┐│
│              │  │ 🎮 FF7R        🔴71  │ │ 🎬 オッペ…    🟡58 ││
│              │  │ 203日 · ¥9,000       │ │ 120日 · ¥2,400      ││
│              │  │「いつ？」             │ │「リストの意味は？」  ││
│              │  │ [✓ 消化した][詳細 →] │ │ [✓ 消化した][詳細→] ││
│              │  └──────────────────────┘ └──────────────────────┘│
│              │   ・・・（スクロール継続）                       │
└──────────────┴─────────────────────────────────────────────────┘
```

**PCサイドバー仕様**

| 項目 | 内容 |
|---|---|
| 幅 | 200px固定、メインコンテンツはフルード |
| ナビ項目 | ホーム / 一覧 / 追加 / 統計 |
| 統計3項目 | サイドバー下部に常時インライン表示 |
| 固定 | スクロール追従（position: sticky） |
| 追加ボタン | サイドバーのナビ項目内に配置（FABなし） |

**PC専用UXポイント**

- **スコア内訳バーを断罪カード内にインライン表示** — 詳細画面に入らず4軸の内訳が一目でわかる
- **統計をサイドバーに常時表示** — ヘッダーはタイトル＋アカウントのみでスッキリ
- **全件リストに検索バー＋ジャンルフィルター追加** — モバイルの並び替えタブに加え絞り込みも可能
- **2列グリッド** — 断罪・全件リストともに2列で情報密度を上げる

---

#### モバイル・PC 差分まとめ

| 要素 | モバイル | PC |
|---|---|---|
| ナビゲーション | ヘッダー＋FAB | 左サイドバー固定（200px） |
| 統計表示位置 | ヘッダー直下（タップで統計画面） | サイドバー下部に常時表示 |
| 断罪件数 | 3件・1列 | **2件・2列グリッド** |
| スコア内訳 | 詳細画面のみ | **断罪カード内にインライン表示** |
| 全件リスト | 1列 | **2列グリッド** |
| 追加ボタン | FAB固定 | サイドバーナビ内ボタン |
| 絞り込み | 並び替えタブのみ | 並び替えタブ＋検索バー＋ジャンルフィルター |

---

### 登録画面

| 項目 | 入力形式 | 必須 |
|---|---|---|
| タイトル | テキスト | ✅ |
| コンテンツタイプ | セレクト（game / book / manga / movie / anime） | ✅ |
| 購入金額 | 数値 | ✅ |
| 購入日 | 日付ピッカー | ✅ |
| 発売日 | 日付ピッカー | ✅（v2でAPI自動取得に移行） |
| プラットフォーム | テキスト（Switch / Steam / Kindle など） | — |
| カバー画像 | URL入力 or ファイルアップロード | — |
| タグ | 自由入力 | — |

---

### 詳細画面

- スコア内訳（4軸の点数をバー表示）
- 毒舌履歴（過去に表示されたセリフ）
- ステータス変更（unplayed / in_progress / completed / abandoned）
- 編集・削除

**`abandoned`（放棄）はスコアリセット対象**  
completedとは別管理し、放棄済みコンテンツは統計の「諦め数」としてカウント。

---

### 統計画面

- 総積み額（未消化合計）
- ジャンル別消化率
- 累計後悔額（purchases - completed金額合計）
- 今月の懺悔数（今月completedにした件数）
- 最長積みランキング

---

## 7. データモデル

### contentsテーブル

```sql
CREATE TABLE contents (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID REFERENCES auth.users NOT NULL,
  title           TEXT NOT NULL,
  type            TEXT NOT NULL CHECK (type IN ('game','book','manga','movie','anime')),
  price           INTEGER NOT NULL,
  purchased_at    DATE NOT NULL,
  release_date    DATE NOT NULL,
  freshness_source TEXT DEFAULT 'manual' CHECK (freshness_source IN ('manual','api')),
  platform        TEXT,
  cover_image_url TEXT,
  status          TEXT DEFAULT 'unplayed' CHECK (status IN ('unplayed','in_progress','completed','abandoned')),
  tags            TEXT[],
  created_at      TIMESTAMPTZ DEFAULT now(),
  updated_at      TIMESTAMPTZ DEFAULT now()
);
```

### roast_historyテーブル（毒舌履歴）

```sql
CREATE TABLE roast_history (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  content_id  UUID REFERENCES contents ON DELETE CASCADE,
  user_id     UUID REFERENCES auth.users,
  roast_text  TEXT NOT NULL,
  score_at    INTEGER,
  shown_at    TIMESTAMPTZ DEFAULT now()
);
```

---

## 8. スコア計算ロジック（TypeScript）

```typescript
type ContentType = 'game' | 'book' | 'manga' | 'movie' | 'anime'

type Content = {
  price: number
  purchased_at: Date
  release_date: Date
  type: ContentType
}

function calcRegretScore(content: Content, sameTypeStats: { total: number; completed: number }): number {
  const now = new Date()
  
  // ① 経過日数スコア（30点）
  const days = Math.floor((now.getTime() - content.purchased_at.getTime()) / 86400000)
  const months = days / 30
  let dayScore = 0
  if (months >= 6)      dayScore = 30
  else if (months >= 5) dayScore = 29
  else if (months >= 4) dayScore = 26
  else if (months >= 3) dayScore = 22
  else if (months >= 2) dayScore = 16
  else if (months >= 1) dayScore = 8

  // ② 消化率スコア（25点）
  const digestRate = sameTypeStats.total > 0
    ? sameTypeStats.completed / sameTypeStats.total
    : 0
  const digestScore = Math.round((1 - digestRate) * 25)

  // ③ 金額スコア（20点）
  let priceScore = 0
  if (content.price >= 5000)      priceScore = 20
  else if (content.price >= 2000) priceScore = 13
  else if (content.price >= 500)  priceScore = 7
  else                            priceScore = 2

  // ④ 話題鮮度スコア（25点）
  const freshMonths = Math.floor(
    (now.getTime() - content.release_date.getTime()) / (86400000 * 30)
  )
  let freshnessScore = 0
  if (freshMonths < 1)       freshnessScore = 25
  else if (freshMonths < 3)  freshnessScore = 20
  else if (freshMonths < 6)  freshnessScore = 13
  else if (freshMonths < 12) freshnessScore = 6

  return Math.min(100, dayScore + digestScore + priceScore + freshnessScore)
}
```

---

## 9. 今後の拡張候補（v2以降）

| 機能 | 概要 |
|---|---|
| コンテンツAPI連携 | RAWG / TMDB / Open Library で発売日・サムネ自動取得 |
| 話題鮮度自動計算 | Google Trends APIで鮮度スコアを動的に更新 |
| プッシュ通知 | スコア急上昇時・長期放置時にWeb Push |
| 売却提案 | 中古相場API連携で「今売ると○○円」を表示 |
| シェア機能 | 「今日の断罪」をX（Twitter）にシェア |
| フレンド機能 | 積み状況を友人と見せ合い、互いに断罪 |
